<!DOCTYPE html>
<html>
<head>
    <?php foreach ($view->getStyles() as $style): ?>
        <link rel="stylesheet" type="text/css" href="<?= $style ?>">
    <?php endforeach ?>
</head>
<body class="wrapper">
    <header>
        <i class="fa fa-search"></i>
    </header>
    <nav>
        <ul>
            <?php foreach ($view->get('adminMenu')->getItems() as $item): ?>
                <li><a href="<?= $router->getUrl($item->getRoute()) ?>"><i class="fa fa-<?= $item->getIcon() ?>"></i><?= $item->getTitle() ?></a></li>
            <?php endforeach ?>
        </ul>
    </nav>

    <main>
        <?= $content ?>
    </main>

    <?php foreach ($view->getScripts() as $script): ?>
        <script type="text/javascript" src="<?= $script ?>"></script>
    <?php endforeach ?>

    <footer>Copyright <?= date('Y') ?></footer>
    <!-- Memory usage: <?= memory_get_usage() / 1024 ?>K -->

</body>
</html>